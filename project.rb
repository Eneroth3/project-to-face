module Project
  def self.project(source_instances, face_path)
    p source_instances
    p face_path
    p face_path.to_a
    puts

    # Iterate instances
    # Create instances inside of face parent (inside its own group)
    # Tag group
    # Calculate flat transformations on face's plane
    # Explode
    # Reset tags?
    # Crop
  end
end